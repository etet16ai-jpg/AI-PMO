# Secrets
.env
*.env
!.env.example

# n8n runtime data (DB, encrypted credentials, encryption key)
n8n_data/
*.sqlite
*.sqlite-*

# Local / editor
.DS_Store
node_modules/
*.log

# Exported credentials — never commit, even "decrypted=false" dumps
credentials*.json
!workflows/**
